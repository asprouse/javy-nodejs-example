{
  "name": "javy-nodejs-example",
  "version": "0.0.1",
  "description": "",
  "main": "host.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "javy emit-provider -o provider.wasm && javy compile embedded.js -d -o embedded.wasm",
    "start": "node --no-warnings=ExperimentalWarning host.mjs"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
